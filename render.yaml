services:
  - type: web
    name: phishguard
    env: python
    buildCommand: pip install -r requirements.txt && python train_model.py
    startCommand: gunicorn app:app
    envVars:
      - key: GROQ_API_KEY
        sync: false